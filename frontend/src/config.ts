declare const process: {
  env: {
    REACT_APP_API_URL?: string;
  }
};

// Temporarily hardcoding the URL to ensure it's using the correct backend
export const API_URL = 'https://test-ydrz.onrender.com/api'; 
