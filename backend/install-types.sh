#!/bin/bash

# Install type definitions
npm install --save-dev @types/node @types/express @types/cors @types/morgan @types/helmet @types/cookie-parser @types/bcryptjs @types/jsonwebtoken @types/qrcode @types/mongoose @types/express-rate-limit @types/zod

# Install other dependencies
npm install express cors morgan helmet cookie-parser bcryptjs jsonwebtoken qrcode mongoose express-rate-limit zod dotenv 